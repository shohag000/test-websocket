package model

// SwapAgreement is the object definition which stores swap information in message thread
type SwapAgreement struct {
	SwapAgreementID string `json:"swapAgreementId" bson:"swapAgreementId"`
}
